# LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*.synctex.gz
*.toc
*.lof
*.lot
*.idx
*.ilg
*.ind
*.xdv

# Keep PDF
!main.pdf

# Local source PDFs live in the central literature store.
paper/sources/**/*.pdf

# Review-board scratch (internal working notes; not for the shared repo)
reviews/

# Local tool state (never for the shared repo)
.freebuff/
.passes.jsonl
