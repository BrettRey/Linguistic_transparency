\section{Projectibility profiles}\label{sec:7}

Section 1 set the paper's central question: when does a transparency attribution support induction, and what stabilizes that induction? In Boydian terms, the question is field-relative: a profile is projectible for a field's explanatory and inductive tasks \autocite{boyd1991,boyd1999}. The primary field here is syntax. The strongest profiles support induction over agreement, syntactic-category licensing, complement selection, and related grammatical behaviour.

This section collects the answers and keeps the field-relative claims separate. The umbrella schema is a tool for locating projectibility profiles, not itself a profile, and the profiles the paper has examined are stabilized by heterogeneous structures.

\subsection{The umbrella isn't a single profile}\label{sec:7-1}

Section \ref{sec:6} established that the umbrella schema is a comparative concept, an analyst's tool for asking the projectibility question of any language. The metaphysical question is what \mention{follows} from posing the question across a heterogeneous family of cases. The umbrella's heterogeneity is the answer. The $X$-values across the family include lexical classes (number-transparent quantificational nouns), constructions (the ditransitive, partly-filled idioms), compounds (NN compounds), and derivationally complex words (-\mention{ness} and -\mention{ost'} nominalizations). The $P$-values include number/count features, constituent meanings and their relations, compositional contributions, and lexical-stem identity. The $R$-values include subject-verb agreement, compositional interpretation, and lexical access; masked priming and transparency ratings are the diagnostics through which the last two are observed, not relations in their own right. No single stabilizing source unifies these triples, and no single field's inductive base supports projection across them.

The umbrella identifies many candidate profiles, not one. A transparency label, on its own, licenses no projection: knowing only that something counts as \mention{transparent}, that the schema's $X$-$P$-$R$ slots are filled, lets us predict nothing substantive about it. The projectible content lives at the level of the individual profile, not at the level of the schema or the label. The schema's role is to make the projectibility question askable, case by case. That role isn't empty. The schema contributes the relational discipline of §\ref{sec:2-3}, the non-instance requirement that $X$ could have made $P$ inaccessible to $R$, and that requirement does analytical work no per-profile description supplies on its own: it excludes coordination as compositional summation (§\ref{sec:2-3}), places frozen idioms at the trivial limit of a real gradient rather than off it (§\ref{sec:3}), and marks where a \mention{transparency} label picks out a genuine mediator rather than restating a distribution. The umbrella isn't a kind; its contribution is this relational discipline.

A useful general distinction follows from this heterogeneity. A \mention{relational family} is a set of cases in which the schema's slots are all filled by something real, while a \mention{projectibility profile} also requires some identifiable source of stability keeping the cluster projectible (a stabilizer in the sense of §\ref{sec:7-3}, not necessarily a causal mechanism). The family isn't a distributional class: its members share no surface distribution, which is precisely the point. The umbrella schema delimits such a family but not a profile: the schema picks out a real family of cases, but no single stabilizing source unifies them at the umbrella level. Each profile examined below has to be evaluated separately for whether it qualifies as projectible in this stronger sense. The negative-polarity-item literature offers a parallel pattern: NPIs share a surface licensing property but the licensing patterns Hoeksema \autocite*{hoeksema-2012-natural-history-npi} catalogues aren't manifestations of one stabilizing source, so NPIs are a class but not a projectibility profile at the umbrella level (developed at length in \autocite[ch.~7]{reynolds2025hpcbook}).

\subsection{Two verdicts, kept apart}\label{sec:7-verdicts}

The word \term{transparency} has been doing three jobs, and separating them makes the framework operational. \term{Applicability} asks whether the case has an identifiable bearer, mediator, $P$, $R$, and concealing baseline at all. The \term{relational verdict} asks, given applicability, how accessible $P$ is to $R$. The \term{profile verdict} asks whether identifying that relation licenses further projection. The three come apart, and Table 4 records them separately for every case the paper has examined.

Table 3 assigns the §\ref{sec:1} roles case by case. Setting them out together is what makes the difference between instantiating the schema and merely resembling its geometry visible: a blank in the bearer, mediator, or baseline column is why a case fails, and the last four rows are blank in the baseline column throughout.

Table 3. Roles filled, case by case. Em-dash marks a role the case doesn't supply.

{\def\LTcaptype{none}\footnotesize
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 8\tabcolsep) * \real{0.17}}
  >{\raggedright\arraybackslash}p{(\linewidth - 8\tabcolsep) * \real{0.21}}
  >{\raggedright\arraybackslash}p{(\linewidth - 8\tabcolsep) * \real{0.17}}
  >{\raggedright\arraybackslash}p{(\linewidth - 8\tabcolsep) * \real{0.23}}
  >{\raggedright\arraybackslash}p{(\linewidth - 8\tabcolsep) * \real{0.22}}@{}}
\toprule\noalign{}
Configuration & Property, and its bearer & Mediator & Relation (diagnostic) & Concealing baseline \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
QN + \mention{of}-complement & number/count profile, borne by the complement & the QN, morphologically mismatched & agreement (verb form) & head-driven agreement \\
\addlinespace
TFR, parent & number profile, borne by the nucleus & \mention{what} & agreement (verb form) & ordinary fused relative \\
\addlinespace
TFR, attributional daughter & syntactic category, borne by the nucleus & \mention{what} & matrix selection (category flexibility) & evidential daughter, NP nuclei only \\
\addlinespace
Collective NP & member plurality, borne by the head itself & --- (same sign) & agreement (verb form) & --- \\
\addlinespace
Coordination & summed number, borne by no single conjunct & --- & agreement (verb form) & --- \\
\addlinespace
Deferred reference & number, borne by the designatum & the subject NP & agreement (verb form) & --- \\
\addlinespace
Spanish partitive subject & number, borne by the complement & \mention{mayoría} & agreement (verb form) & singular option, dispreferred not blocked \\
\addlinespace
Tsez complement clause & noun class, borne by the embedded absolutive & the clause & matrix agreement (class prefix) & class IV, the clausal default \\
\addlinespace
Korean dative experiencer & subjecthood, borne by the NP itself & dative case & disputed: honorification is a relation only if it is agreement (stacking, \mention{caki}, float are diagnostics) & no independently motivated family available \\
\addlinespace
Frozen idiom & constituent meaning, borne by the parts & the construction & interpretation (paraphrase) & compositional sibling constructions \\
\addlinespace
NN compound & constituent meaning, borne by the constituents & the compound & compositional interpretation (rated transparency) & opaque compounds \\
\addlinespace
Derived word & stem identity, borne by the stem & the derived form & lexical access (priming magnitude) & opaque derivatives \\
\addlinespace
Aleut (App.~\ref{sec:B}) & person/number, borne by an unpronounced possessor & the possessed NP & anaphoric verb morphology plus subject case & overt-possessor alternant \\
\addlinespace
Lardil/Kayardild (App.~\ref{sec:B}) & clausal TAM, borne by the clause & the dependency domain & dependent-nominal morphology & --- \\
\addlinespace
Chamorro (App.~\ref{sec:B}) & grammatical relation of the extractee & the extraction path & path morphology & --- \\
\addlinespace
Obviation (App.~\ref{sec:B}) & possessor obviation, borne by the possessor & the possessed NP & --- : the predicate tracks the head's own derived feature & --- \\
\end{longtable}
}

Table 4. Relational and profile verdicts.

{\def\LTcaptype{none}
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.26}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.37}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.37}}@{}}
\toprule\noalign{}
Case & Relational verdict & Profile verdict \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
Number-transparent QNs (§\ref{sec:2-1}) & Applicable, transparent & Strongest candidate; within-domain projection over known members, plus one candidate-member test (\mention{piles}) with the determiner-frame prediction confirmed and the agreement prediction untestable for sparsity \\
\addlinespace
Transparent free relatives (§\ref{sec:2-5}) & Applicable, transparent at parent (agreement) and daughter (selection) & Second syntactic candidate, conditional on Kim's constructional analysis \\
\addlinespace
Collective NPs (§\ref{sec:2-2}) & Near case: bearer and mediator are layers of one sign, so not inter-element access & None claimed; the override is construal-sensitive rather than class-based \\
\addlinespace
Ordinary coordination (§\ref{sec:2-3}) & Not applicable: no mediator, since nothing conceals the summed features & None \\
\addlinespace
Deferred reference (§\ref{sec:2-3}) & Not applicable: no bearer within $X$, the property being sourced from the designatum & None \\
\addlinespace
Spanish partitive subjects (§\ref{sec:6-topologies}) & Applicable, transparent & Not shown: the access pattern travels, the English cluster hasn't been tested \\
\addlinespace
Tsez long-distance agreement (§\ref{sec:6-topologies}) & Applicable, transparent & None claimed \\
\addlinespace
Korean dative subjects (§\ref{sec:6-korean}) & Underdetermined: candidate diagnostics without a securely identified $R$ & No verdict available \\
\addlinespace
Frozen idioms (§\ref{sec:3}) & Applicable, opaque at or near degree zero & The gradient supports projection; the individual idiom doesn't \\
\addlinespace
NN compounds (§\ref{sec:3}) & Applicable, gradient; $R$ = compositional interpretation, rated transparency the diagnostic & Within-domain, on corpus-statistical predictors \\
\addlinespace
Derived words (§\ref{sec:3}) & Applicable, gradient; $R$ = lexical access, priming magnitude the diagnostic & Within-domain, at long SOAs only \\
\end{longtable}
}

Two things follow. A case can be applicable and still score at the opaque end, which is why a frozen idiom sits on the gradient rather than off it, where coordination and deferred reference are off it entirely for want of a mediator and a bearer respectively. And a profile verdict can fail while the relational verdict stands: the thin inanimate \mention{bunch} result (§\ref{sec:2-2}) weakens a proposed corpus-level regularity without touching \textit{CGEL}'s grammatical claim.

\subsection{Which fields the profiles belong to}\label{sec:7-2}

Table 4 records the verdicts; what it doesn't show is which field each profile is projectible \mention{for}, and that matters because projectibility is field-relative. The two syntactic profiles have observable bases a syntactician can apply: lexical class membership for the QNs, identified by quantificational semantics and \mention{of}-complement distribution as well as by lexical identity, and constructional sub-type plus verb-class membership for the TFRs. Both project to grammatical behaviour, which is the field the paper claims.

The form--meaning profiles belong to neighbouring fields, and their bases are correspondingly different: corpus-statistical predictors for compounds, rated transparency for derived words. Each licenses within-domain projection. Whether the three compose into a cross-domain cluster on shared items is an open empirical question that the available evidence doesn't settle either way, and a within-items probe (corpus predictors, ratings, priming, interpretation accuracy on the same items), designed in separate work, would test it. The form--meaning family is an open research programme pursued elsewhere, not a delivered result of this paper.

\subsection{Stabilizing structures}\label{sec:7-3}

Different structures stabilize the profiles, and the paper's $R$-values across §§\ref{sec:2}--\ref{sec:3} call for a small inventory of stabilizing sources rather than a single mechanism.

\term{Lexical convention} stabilizes the number-transparent nouns as a class. Lexical inheritance here means that speakers acquire not isolated words but item-specific constructional slots: \mention{a lot of} X, \mention{plenty of} X, \mention{the rest of} X, \mention{a number of} X, with their associated agreement and complement-selection behaviour. The acquisition target is the slot, not just the lexeme. Speakers acquire a small, relatively stable inventory of these slots; new members enter at the boundary (\mention{majority, minority}) or by sense-split (\mention{couple}₂). Class identity itself isn't held by Boyd-style homeostatic feedback; it has the stability characteristic of any small, conventionally established grammatical class, and the override it licenses is a property of that construction, corroborated by the negative control in Appendix~\ref{sec:A-5}. The count/non-count percolation behaviour the class licenses connects to broader patterns in English countability; \autocite{reynolds2026countability} develops a Khalidian causal-cluster analysis of those patterns with conventional individuation as a central node. The transparency paper draws on that analysis but doesn't depend on its stronger claims; the within-NP percolation can be observed and predicted independently.

\term{Constructional inheritance} stabilizes the TFR daughter profile (§\ref{sec:2-5}) and the network-relative transparency hypothesis of §\ref{sec:3}. In TFRs, the parent and attributional daughter support different access conditions. In the form--meaning family, a construction's compositional accessibility depends partly on the inheritance hierarchy it sits in; constructions whose inherited supercategories are well-attested support stronger compositional projection than orphaned constructions.

\term{Usage-based entrenchment} stabilizes the corpus-statistical predictors of compound transparency (§\ref{sec:3}). Bell and Schäfer's frequency, productivity, and semantic-relation expectedness predictors are corpus-derivable measurements of usage entrenchment; the rated-transparency target is a steady-state property whose stability is plausibly a reflex of that entrenchment.

\term{Processing architecture} stabilizes the time-course modulation of priming (§\ref{sec:3}). Heyer and Kornishova's SOA-dependent transparency effect is a processing-architecture observation: the time-course of morphological decomposition makes morpho-orthographic information available before morpho-semantic information, and the resulting priming pattern depends on representational properties that the processing architecture exposes only at certain stages.

The list isn't closed. Other linguistic profiles may be stabilized by combinations of these or by sources the paper doesn't invoke (information-structural pressures, learnability constraints, normative/pedagogical regularization). The point is that \enquote{what stabilizes this profile?} is the right empirical question, and the answer is heterogeneous.

\subsection{SPC and Khalidi as diagnostics}\label{sec:7-4}

SPC and Khalidi are complementary diagnostics rather than nested ones. Slater asks whether the cluster is stable and projection-supporting and is neutral on \mention{what} stabilizes it. Khalidi asks whether the stabilizing structure is itself causal-hierarchical, with named candidate core properties whose causal action generates derivative properties. A profile can satisfy SPC without satisfying Khalidi, and (in principle) the reverse. The \term{projectibility profile} is the unit of analysis; the two diagnostics ask different questions of it.

Slater's \autocite*{slater-2015-natural-kindness} stable property cluster (SPC) account gives the stability-plus-projection diagnostic. On it, a category qualifies when its cluster of properties is, in Slater's phrase, \enquote{sufficiently stably coinstantiated to accommodate the inferential and explanatory uses} a science puts the category to, regardless of \mention{what} maintains the stability. The affirmative content is that the stability has to be \term{cliquish}: possessing some of the cluster's properties reliably (if imperfectly) indicates the rest, so that observing a few licenses projection to the others. Number-transparent quantificational nouns are the strongest candidate to satisfy SPC examined here. Concretely, cliquishness means that a noun showing a few of the class's properties (the \mention{a lot of} determiner frame, a strong preference for the bare \mention{of}-complement, the agreement override onto its complement) is a good bet to show the rest; that is the sub-group covariance §\ref{sec:2-4} documents, where determiner-pattern, partitive availability, and override-direction co-vary by sub-group. The maintenance is at least conventional/lexical for class identity (§\ref{sec:7-3}). The within-domain audit beyond override direction is empirically open: the cliquishness claim is licensed by that covariance, not yet by an end-to-end corpus or experimental study.

TFRs are a second syntactic candidate. The parent/daughter split and the verb-class restriction form a projected cluster stabilized by constructional inheritance, though the present evidence is a working corpus check rather than a completed profile audit. Each form--meaning window (§\ref{sec:3}) is a candidate to satisfy SPC within its own field, pending the within-domain audits. Whether the family as a whole satisfies SPC at the cross-domain level is the open empirical question §\ref{sec:7-2} flags.

Khalidi \autocite*{khalidi-2018-causal-networks} applies where the stabilizing structure is ordered and causal-hierarchical, with named core properties whose causal action generates derivative properties via identifiable pathways. QN class identity isn't such a case: it's conventionally stabilized without a separate ordered causal hierarchy, so SPC, not Khalidi, is the diagnostic that fits it. One level out, the count cluster the QN's \mention{of}-complement feeds (\mention{a lot of money} non-count, \mention{a lot of tourists} count) has been argued to be causal-hierarchical, with conventional individuation as a candidate core property \autocite{reynolds2026countability}. That analysis belongs to the countability paper and isn't needed here: the within-NP percolation the QN profile relies on can be observed and predicted under SPC alone. Khalidi names the vocabulary for asking whether a sub-profile has internal causal ordering; it isn't a result this paper delivers for the QN case.

For the form--meaning family of §\ref{sec:3}, Khalidi is acknowledged as a possible refinement \mention{if} corpus predictors, rated transparency, priming magnitude, and interpretation accuracy align on shared items as joint reflexes of underlying representational structure. These four measures aren't a temporal chain (masked priming probes early lexical access before explicit interpretation, and ratings are a late metalinguistic judgement) but candidate joint consequences of a single underlying organization. The within-items evidence doesn't yet exist; separate work sketches a within-items probe design. Until that probe is run, Khalidi remains a possible refinement for the form--meaning family rather than a delivered analysis.

Kindhood, on this picture, is the downstream verdict. A profile that scores well on the SPC diagnostic, and especially one that also scores well on the Khalidi diagnostic, has earned the kind label. A profile that scores modestly is a candidate. A profile that fails the SPC diagnostic isn't a kind, even if it remains a useful descriptive category. Failing Khalidi's is not disqualifying, since the two aren't nested: a conventionally stabilized cluster can be stably coinstantiated without its stabilizers forming an ordered causal hierarchy, which is exactly the QN case. What supports projection is the stabilizing structure; the diagnostics assess whether it does so well enough, and the kind label follows from that assessment.

The apparatus matters in two places. SPC's maintenance-neutrality lets the paper put conventional, entrenched, architectural, and formal-semantic stabilizers on the same footing, where Boyd's homeostasis would force one mechanism (a poor fit for lexical convention). Khalidi's causal-hierarchy diagnostic gives the paper a vocabulary for asking whether sub-profiles have internal ordering: the form--meaning family's possible cross-modal cluster is the candidate case.

\subsection{The resulting claim}

Linguistic \term{transparency} isn't a kind, and the umbrella schema doesn't aspire to be one. Applying the label predicts nothing on its own: that something is \mention{transparent} says the schema's slots are filled, not what the filling lets you infer, so what supports induction is the specific profile a transparency claim points to, not the word. The schema is a diagnostic tool for locating field-relative projectibility profiles: cases where an observable base licenses non-trivial projection to a target, stabilized by some identifiable source. Different transparency profiles are stabilized differently (lexical convention, constructional inheritance, usage entrenchment, processing architecture, formal-semantic environment), and each profile's projectibility is what makes the transparency attribution scientifically useful, not the profile's classification within any single ontology of kinds.

Three levels stay distinct, per §\ref{sec:6}:

\begin{itemize}
\tightlist
\item
  \term{Comparative concept}: mediated accessibility, the cross-linguistic methodological tool defined by the schema of §\ref{sec:1}.
\item
  \term{Descriptive category}: language-particular grammatical content. English number-transparent quantificational nouns are a \textit{CGEL} descriptive category; English NN compounds and -\mention{ness} nominalizations are descriptive categories defined by language-particular criteria; Korean case-stacking and scrambling are Korean-particular descriptive categories; and so on.
\item
  \term{Projectibility profile}: an observable base that licenses non-trivial projection to a target, stabilized by some identifiable source, realized within or across language-particular descriptive categories. This is the projectible profile of §\ref{sec:7-1}: a maintained-and-projectible cluster, contrasted with a \mention{class} (mere distributional grouping). Profiles that score well on the SPC and (where appropriate) Khalidi diagnostics earn the local-kind label as a downstream verdict.
\end{itemize}

The contribution here is to apply the projectibility-first move to a \mention{relational} concept. \term{Transparency} is the relation $X$-mediates-$P$-for-$R$, not an entity. Treating a relation this way needs something a property-kind analysis doesn't: a way to tell genuine mediation from cases where no property is being made accessible at all. The non-instance requirement of §\ref{sec:2-3} supplies it, which is why the projectibility-first move transfers to a relational concept instead of dissolving into a list of co-occurrences. Asking \mention{when does this attribution support induction, and what stabilizes the induction?} works for relations as it does for the umbrella concepts \mention{language} and \mention{grammatical category} addressed in related work. The paper asks that naturalization question of transparency too, and answers it locally, profile by profile, rather than at the umbrella level.
