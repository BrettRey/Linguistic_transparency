# Fix and editorial-scar-tissue plan
This checkpoint covers `main.tex` and its inputs in _Access transparency in grammar_, plus `two-kinds.tex` in _Two Kinds of Transparency_. The empirical counts and classifications stay fixed.
## Required corrections
1. Restore the empty quotation in the typology objection as `just a question`, but rewrite the sentence directly rather than preserving the repair-shaped `The reply isn't ... The reply is ...` frame.

2. Change the appendix reference for the _piles_ prospective test from §2.5 to §2.4.

3. Update the umbrella's residual-work language: naturally occurring category-discriminating environments are already present; systematic judgments and adjudication of rival analyses remain.

4. Add a local bibliography entry for the 22 August working paper under a new key and keep the existing WRAPP-presentation key intact.

5. Replace `like-for-like` with a descriptive row-level comparison in both papers. Keep `candidate rows`, not `tokens`, for the 733 denominator, and state that the roughly seventy-fold figure is descriptive.

6. Standardize the pair on `AdjP` rather than umbrella `AP`.

7. Define `mediated accessibility` briefly in the umbrella abstract so the subtitle and keywords are earned.

8. Bring the companion's strong PP candidate into line with the audit: deletion-passing and predicative, but with a coexisting nominal/repair analysis.

9. Replace `full coding`/`coded in full` with the exact 485-row coverage.

10. Repair the companion's incomplete headline-rate sentence.

11. Rephrase the companion passages responsible for the three overfull boxes, especially the 5.56 pt box in §3.3.


The earlier proofread report's proposed plural agreement with _data_ was wrong under the current house guide, which treats _data_ as a mass noun. The manuscript's `data refutes` and `data shows` stay unchanged, and the report will be corrected.
## Editorial scar tissue to remove
### Umbrella paper
- Rewrite the applicability/falsification distinction in the introduction as a direct statement of the protocol's scope.

- Replace `Our aim is constructive rather than exhaustive` with a direct scope sentence.

- State framework portability positively rather than beginning with `Nothing ... depends on phrase structure`.

- Replace `It is not a blocked one` with the fresh description `marked but attested`.

- State the unresolved _rest_ classification directly from what the corpus establishes.

- Recast the _bunch_ corpus result as evidence about projectibility without relitigating the grammaticality of CGEL's constructed example.

- Remove `not an ad hoc filter` and state what the counterpart clause diagnoses.

- Replace `a real limitation ... not a detail about its edges` with a direct statement of the limitation.

- Replace `does not validate the full profile` and `not a completed projection test` with positive scope statements.

- Delete the appendix sentence about `what was wrong in the earlier draft`; state the corrected semantic analysis directly.

- Keep corrective contrasts in the Haspelmath/Newmeyer objection section where a cited opponent or explicit objection supplies the discourse target.

### Companion paper
- Delete the meta-headings `A note on scope`, `A note on evidential status`, and `requires a caveat`; state the content directly.

- Remove the superseded March-count history from the table caption. The archive, not the article, carries that repair history.

- Combine the short `This matters ... It doesn't. It patterns ...` sequence into one direct inference from _take to be_.

- Replace `note that the token is typographically set off` with the observation itself.

- State the metalinguistic account directly: the frame attributes a label and thereby admits categories excluded from ordinary predication.

- State the agreement diagnostic's domain directly rather than announcing that its limits are `worth stating`.

- Replace the §5 opening about what the section `previously listed` and what the August campaign `discharged` with `Four items remain`.

- Keep genuine hypothesis contrasts, rival analyses, negative corpus results, and explicit scope restrictions. They serve the argument and aren't revision residue.

## Verification
- Run the house-style checker and terminology checker on both manuscripts.

- Rebuild both PDFs with XeLaTeX.

- Check logs for undefined references/citations and overfull boxes.

- Re-read every changed paragraph under the end-reader test and record the editorial-scar-tissue pass against the exact revised text where the pass driver can identify the correct manuscript root.
